--- origsrc/e2fsprogs-1.41.6/configure.in	2009-04-22 07:47:39.000000000 -0500
+++ src/e2fsprogs-1.41.6/configure.in	2009-06-21 21:11:28.747174200 -0500
@@ -227,13 +227,17 @@ else
 	E2_PKG_CONFIG_STATIC=
 	ELF_CMT=
 	MAKEFILE_ELF=$srcdir/lib/Makefile.elf-lib
+	LIB_EXT=.so
 	[case "$host_os" in
+	cygwin*)
+		MAKEFILE_ELF=$srcdir/lib/Makefile.cygwin-lib
+		LIB_EXT=.dll.a
+	;;
 	solaris2.*)
 		MAKEFILE_ELF=$srcdir/lib/Makefile.solaris-lib
 	;;
 	esac]
 	BINARY_TYPE=elfbin
-	LIB_EXT=.so
 	PRIVATE_LIBS_CMT=#
 	LDFLAG_DYNAMIC=['-Wl,-rpath-link,$(top_builddir)/lib']
 	AC_MSG_RESULT([Enabling ELF shared libraries])
--- origsrc/e2fsprogs-1.41.6/lib/Makefile.cygwin-lib	1969-12-31 18:00:00.000000000 -0600
+++ src/e2fsprogs-1.41.6/lib/Makefile.cygwin-lib	2009-06-22 02:51:35.848631800 -0500
@@ -0,0 +1,63 @@
+#
+# This is a Makefile stub which handles the creation of Cygwin shared
+# libraries.
+#
+# In order to use this stub, the following makefile variables must be defined.
+# 
+# ELF_VERSION = 1.0
+# ELF_SO_VERSION = 1
+# ELF_IMAGE = libce
+# ELF_MYDIR = et
+# ELF_INSTALL_DIR = $(SHLIBDIR)
+# ELF_OTHER_LIBS = -lc
+
+all:: image
+
+real-subdirs:: Makefile
+	@echo "	MKDIR elfshared"
+	@mkdir -p elfshared
+
+CYGWIN_DLL = $(ELF_IMAGE:lib%=cyg%)-$(ELF_SO_VERSION).dll
+CYGWIN_IMPLIB = $(ELF_IMAGE).dll.a
+
+image:		$(CYGWIN_DLL)
+
+$(CYGWIN_DLL): $(OBJS)
+	@echo "	GEN_CYGWIN_DLL $(CYGWIN_DLL)"
+	@(cd elfshared; $(CC) --shared -o $(CYGWIN_DLL) $(LDFLAGS) \
+		-Wl,--enable-auto-image-base,--out-implib,$(CYGWIN_IMPLIB) $(OBJS) $(ELF_OTHER_LIBS))
+	@$(MV) elfshared/$(CYGWIN_DLL) elfshared/$(CYGWIN_IMPLIB) .
+	@$(RM) -f ../$(CYGWIN_IMPLIB)
+	@$(LN) $(CYGWIN_IMPLIB) ../$(CYGWIN_IMPLIB)
+
+installdirs-elf-lib::
+	@echo "	MKINSTALLDIRS $(ELF_INSTALL_DIR) $(bindir)"
+	@$(MKINSTALLDIRS) $(DESTDIR)$(ELF_INSTALL_DIR) \
+		$(DESTDIR)$(bindir)
+
+installdirs:: installdirs-elf-lib
+
+install-shlibs install:: $(CYGWIN_DLL) installdirs-elf-lib
+	@echo "	INSTALL-CYGWIN-DLL $(bindir)/$(CYGWIN_DLL)"
+	@$(INSTALL_PROGRAM) $(CYGWIN_DLL) $(DESTDIR)$(bindir)/$(CYGWIN_DLL)
+	@echo "	INSTALL-CYGWIN-IMPLIB $(ELF_INSTALL_DIR)/$(CYGWIN_IMPLIB)"
+	@$(INSTALL_DATA) $(CYGWIN_IMPLIB) $(DESTDIR)$(ELF_INSTALL_DIR)/$(CYGWIN_IMPLIB)
+
+install-strip: install
+	@echo "	STRIP-DLL $(bindir)/$(CYGWIN_DLL)"
+	@$(STRIP) --strip-unneeded --remove-section=.comment \
+		--remove-section=.note $(DESTDIR)$(bindir)/$(CYGWIN_DLL)
+
+install-shlibs-strip: install-shlibs
+	@echo "	STRIP-DLL $(bindir)/$(CYGWIN_DLL)"
+	@$(STRIP) --strip-unneeded --remove-section=.comment \
+		--remove-section=.note $(DESTDIR)$(bindir)/$(CYGWIN_DLL)
+
+uninstall-shlibs uninstall::
+	$(RM) -f $(DESTDIR)$(bindir)/$(CYGWIN_DLL) \
+		$(DESTDIR)$(ELF_INSTALL_DIR)/$(CYGWIN_IMPLIB)
+
+clean::
+	$(RM) -rf elfshared
+	$(RM) -f $(CYGWIN_DLL)
+	$(RM) -f ../$(CYGWIN_DLL) ../$(CYGWIN_IMPLIB)
--- origsrc/e2fsprogs-1.41.6/lib/uuid/Makefile.in	2009-02-14 07:49:08.000000000 -0600
+++ src/e2fsprogs-1.41.6/lib/uuid/Makefile.in	2009-06-21 21:13:15.340434300 -0500
@@ -176,6 +176,7 @@ distclean:: clean
 # Hack to parallel makes recognize dependencies correctly.
 #
 ../../lib/libuuid.a: libuuid.a
+../../lib/libuuid.dll.a: image
 ../../lib/libuuid.so: image
 ../../lib/libuuid.dylib: image
 
